// Single source for app identity strings shown in the UI.
// Keep appVersion in step with the version in pubspec.yaml.
class AppInfo {
  const AppInfo._();

  static const String name = 'MaisNutri';
  static const String version = '1.0.0';
}
